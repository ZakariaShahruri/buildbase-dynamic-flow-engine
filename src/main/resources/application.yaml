spring:
  profiles:
      active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: flow-engine
  data:
    mongodb:
      database: ${MONGODB_DATABASE}
      uri: ${MONGODB_URI}
server:
  port: 8080
